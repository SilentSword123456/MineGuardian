# Minecraft Backend (Flask + Socket.IO)

This directory is reserved for the backend app migration from `MineGuardian-Backend`.

## Planned content

- Flask app entrypoint (`main.py`)
- Modular services (`services/*`)
- Database modules (`Database/*`)
- Minecraft process management modules
- Backend tests (`tests/*`)

## Temporary note

Until backend code is imported, workspace scripts that start the backend may fail because `main.py` is not present yet.
