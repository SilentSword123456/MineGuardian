# Control-plane API contract (draft)

Planned operations from Convex/Next server actions to Flask backend:

- install server
- start server
- stop server
- restart server
- uninstall server
- read server status/resources
- manage permissions

Shared request/response contracts should be defined in `packages/shared`.
